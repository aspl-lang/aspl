[public]
[static]
class TypeUtils {
    
    [public]
    [static]
    method shortName(string s) returns string{
        if(s == "integer"){
            return "int"
        }elseif(s == "boolean"){
            return "bool"
        }else{
            return s
        }
    }

}